[
    {
        "type": "function",
        "function": {
            "name": "web_search",
            "description": "Search the internet for real-time information, current events, news, facts outside your training data, or any general question that you can't properly answer on your own.",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "A specific, keyword-rich search query optimized for a search engine."
                    }
                },
                "required": ["query"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "open_link",
            "description": "Retrieve the full text content of a specific URL. Use this to read a specific article, documentation page, or website provided by the user, or to deep-dive into a search result that looks promising.",
            "parameters": {
                "type": "object",
                "properties": {
                    "url": {
                        "type": "string",
                        "description": "The full, valid URL to fetch (e.g., https://example.com/article)."
                    }
                },
                "required": ["url"]
            }
        }
    }
]